#cloud-config
package_update: true
package_upgrade: true

packages:
  - ca-certificates
  - curl
  - gnupg
  - git
  - build-essential

# Mechanical, safe-to-run-unattended prep only. Installing OpenClaw itself
# and pairing WhatsApp/Telegram needs a human (QR code / prompts), so that
# happens over SSH afterward, against the live official installer rather
# than a copy baked in here.
runcmd:
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - >
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  - usermod -aG docker ubuntu
  - systemctl enable --now docker
  - mkdir -p /opt/openclaw
  - chown ubuntu:ubuntu /opt/openclaw
  - touch /opt/openclaw/READY_FOR_ONBOARDING
  # Auto-updates any running container (OpenClaw included, once deployed)
  # so ongoing maintenance doesn't require anyone SSHing in or Claude
  # relaying commands. Checks daily, prunes old images after updating.
  - >
    docker run -d --name watchtower --restart unless-stopped
    -v /var/run/docker.sock:/var/run/docker.sock
    containrrr/watchtower --interval 86400 --cleanup
